set(DIRECTORY_TO_LIST ${PROJECT_SOURCE_DIR}/src)
message( "DIRECTORY_TO_LIST----> ${DIRECTORY_TO_LIST}")
file(GLOB ALL_ITEMS LIST_DIRECTORIES true  ${PROJECT_SOURCE_DIR}/src/*  )
foreach(ITEM ${ALL_ITEMS})
    if(IS_DIRECTORY ${ITEM})
        # message( STATUS "found dir -> ${ITEM}")
        add_subdirectory(${ITEM})
    endif()
endforeach(ITEM)
